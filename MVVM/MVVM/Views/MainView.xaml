<Window x:Class="MVVM.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MVVM.ViewModels.View"
        Title="Audio Player" Height="350" Width="525">


	<Window.DataContext>
		<local:PlayerViewModel />
	</Window.DataContext>

	<Grid>

		<Button Content="Play" Command="{Binding PlayCommand}" HorizontalAlignment="Left" Margin="0,215,0,0" Width="73" />
		<Button Content="Pause" Command="{Binding PauseCommand}" HorizontalAlignment="Center" Margin="0,215,0,0" Width="72" />
		<Button Content="Stop" Command="{Binding StopCommand}" HorizontalAlignment="Right" Margin="0,215,0,0" Width="79" />

		<TextBlock Text="{Binding CurrentAudio.FileName}" Margin="0,30,0,0" HorizontalAlignment="Center" />
	</Grid>
</Window>